spring:
  datasource:
    url: ${JDBC_DATABASE_URL:jdbc:mysql://localhost:3306/Horb_db?serverTimezone=UTC}
    username: ${JDBC_DATABASE_USERNAME:ari100tel}
    password: ${JDBC_DATABASE_PASSWORD:lop00100pol}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
#server:
 # port: 8081
management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  metrics:
    export:
      prometheus:
        enabled: true
    distribution:
      percentiles-histogram:
        "[http.server.requests]": true
